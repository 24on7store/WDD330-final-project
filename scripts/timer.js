// timer.js
document.addEventListener("DOMContentLoaded", () => {
    const timerBox = document.getElementById("timer");
    if (!timerBox) return; // Not on quiz page

    console.log("Timer module ready");
});
